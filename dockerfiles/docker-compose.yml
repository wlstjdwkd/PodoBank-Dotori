version: "3"
services:
  podobank:
    image: podobank:${BUILD_NUMBER}
    build:
      context: ../Podo_Bank/backend
      dockerfile: Dockerfile.Podo_back
      args:
        - BUILD_NUMBER
    ports:
      - "9000:9000"
    environment:
      - SPRING_REDIS_HOST=redis
    depends_on:
      - redis
  redis:
    image: redis:${BUILD_NUMBER}
    ports:
      - "9050:6379"
    volumes:
      - /etc/redis/redis.conf:/usr/local/etc/redis/redis.conf
    command: [ "redis-server", "/usr/local/etc/redis/redis.conf" ]

  #redis_dotori:
    #image: redis_dotori