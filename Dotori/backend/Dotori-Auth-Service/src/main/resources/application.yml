server:
  port: 9110
# http://j9d107.p.ssafy.io:9900/
spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      name: dotori
      profile: user
  application:
    name: dotori-auth-service

encrypt:
  key: ${ENCRYPT_KEY}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

dotori:
  auth:
    url: http://localhost:9110/
  user:
    url: http://localhost:9120/
  purpose:
    url: http://localhost:9130/
  main:
    url: http://localhost:9140/